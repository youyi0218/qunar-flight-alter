# ctrip-flight-alter

???????????

## ??

- ?? `url.txt` ?????????? URL
- ?????? URL ????? / ??? / ??????? `config.json.routes` ??????????
- ?????????????????????????
- ???????????/???????????????????
- ?????????
- ????????
- ????????????????????
- ????????????????????????? 3 ???
- ?? PushPlus HTML ?? + Resend ????
- ????????

## ??

1. ????

```bash
pip install -r requirements.txt
```

2. ?????  
   ???????? Chromium / Chrome???? `config.json -> browser.executable_path` ?????

3. ?? `url.txt`  
   ??????????? URL???????????????

```text
????? ????? ?????5?1? https://flights.ctrip.com/online/list/oneway-hak-wuh?depdate=2026-05-01&cabin=y_s_c_f&adult=1&child=0&infant=0
```

4. ??????? cookie  
   ?????????????????? **Cookie-Editor** ??? `cookie.json`??????????????

5. ????

```bash
cp config.example.json config.json
```

## ??

?????

- `url_file`
- `pushplus.token`
- `email.api_key`
- `email.from`
- `email.to`
- `service.schedule_times`
- `service.capture_lead_minutes`
- `routes`

???

- `url_file` ?? `url.txt`
- `routes` ?????????? `expected_price` / `enabled`
- `routes` ???? `source_url` ???????? `source_url`??? `???? + ???? + ????` ?? `url.txt` ?????????

?????

```text
???? ?? ????
```

???

```text
03?31? ????? 2026-05-05
```

## ??

?????

```bash
python flight_monitor.py
```

???????

```bash
python flight_monitor.py --dry-run --dump-json
```

?????

```bash
python flight_monitor.py --service
```

Service mode captures prices before the configured schedule window and pushes at the configured `schedule_times`.

## Docker

### ????

```bash
docker build -t ctrip-flight-alter .
docker run --rm   --user root   -v $(pwd):/app   ctrip-flight-alter
```

### ?? GitHub Action ????????

??????????????

```text
ghcr.io/youyi0218/ctrip-flight-alter:latest
```

??????

```bash
docker pull ghcr.io/youyi0218/ctrip-flight-alter:latest
docker run -d   --name ctrip-flight-alter   --restart unless-stopped   --user root   -v $(pwd):/app   ghcr.io/youyi0218/ctrip-flight-alter:latest
```

### Docker Compose ??

????? `docker-compose.yml`???**??????**??????? `/app`?

??????????????????

- `config.json`
- `url.txt`
- `cookie.json`
- `.flight_monitor_history.json`
- `.flight_monitor_state.json`

????????????????????????????????

- `flight_monitor.py`
- `config.example.json`
- `README.md`
- `requirements.txt`
- `docker-compose.yml`
- `Dockerfile`
- `.gitignore`
- `.dockerignore`
- `docker-entrypoint.sh`
- `LICENSE`

????????? `config.json`???????? `config.example.json` ?????

?????

```bash
docker login ghcr.io -u youyi0218
docker compose pull
docker compose up -d
```

????????????

```bash
# ??????????? config.json
# ?? url.txt / cookie.json ???????cookie ???
docker compose restart
```

?????

```bash
docker compose logs -f
```

???

```bash
docker compose down
```

????????????????? `docker compose` ?? `docker-compose`?

?????????

```bash
python /app/flight_monitor.py --service --config /app/config.json
```

## GitHub Actions

??? Docker ??????

- `push`
- `workflow_dispatch`
